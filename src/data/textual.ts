// /!\ This module will be replaced at build time, just before we launch the Next.js build!
import { ProjectGalleryItem } from "../domain"

const gallery: ProjectGalleryItem[] = []
export default gallery
